# cn-service-rule-stream —— **host** 覆盖层（application-configuration.md §7）。
# 加载顺序：application.yml → 本文件 → 环境变量 → 系统属性。
app:
    rule-stream:
        streams:
            bootstrap-servers: 192.168.122.1:19092
        mysql:
            host: 192.168.122.1
            port: 8045
            user: root
            password: ss
        redis:
            connection-string: redis://:ss@192.168.122.1:6380/6
