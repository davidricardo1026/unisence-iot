# cn-service-rule-stream —— **dev** 覆盖层（application-configuration.md §7）。
# 加载顺序：application.yml → 本文件 → 环境变量 → 系统属性。
app:
    rule-stream:
        streams:
            bootstrap-servers: 127.0.0.1:19092
        mysql:
            host: 127.0.0.1
            port: 8045
            user: root
            password: ss
        redis:
            connection-string: redis://:ss@127.0.0.1:6380/6
