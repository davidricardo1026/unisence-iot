# cn-service-admin-init —— **dev**（初始化 CLI）。
logging:
    config: classpath:log4j2-dev.xml
spring:
    datasource:
        url: jdbc:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:8045}/${MYSQL_DATABASE:unisence_iot}?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
        username: ${MYSQL_USERNAME:root}
        password: ${MYSQL_PASSWORD:ss}
app:
    init:
        greptime:
            jdbc-url: ${GREPTIME_INIT_JDBC_URL:jdbc:mysql://127.0.0.1:4002/?useSSL=false&serverTimezone=UTC&connectionTimeZone=UTC}
            user: ${GREPTIME_USER:}
            password: ${GREPTIME_PASSWORD:}
